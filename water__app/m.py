import streamlit as st
from datetime import date

# --------------------
# 페이지 설정
# --------------------
st.set_page_config(
    page_title="물 한 컵 챌린지",
    page_icon="👏",
    layout="centered"
)

# --------------------
# 오늘 날짜 표시
# --------------------
today = date.today()
st.caption(f"📅 오늘 날짜: {today}")

# --------------------
# 앱 제목 및 설명
# --------------------
st.title("💧 물 한 컵 챌린지")
st.write("작은 실천으로 환경을 지켜요!")

# --------------------
# 체크 항목
# --------------------
st.subheader("✅ 오늘의 물 절약 실천")
check_cup = st.checkbox("양치할 때 컵 사용하기")
check_shower = st.checkbox("샤워 시간 줄이기")
check_tap = st.checkbox("사용하지 않을 때 수도 잠그기")

# --------------------
# 체크 개수 계산
# --------------------
count = sum([check_cup, check_shower, check_tap])

# --------------------
# 실천 완료 버튼
# --------------------
if st.button("실천 완료"):
    if count == 0:
        st.info("아직 시작 전이에요. 오늘 하나만 실천해볼까요? 😊")
        st.image("start.png", width=200)  # 시작 전 이미지

    elif count == 1:
        st.success("좋은 시작이에요! 👏")
        st.image("clap_small.png", width=200)  # 작은 박수 이미지

    elif count == 2:
        st.success("와, 정말 잘하고 있어요! 👏👏")
        st.image("clap_medium.png", width=200)  # 큰 박수 이미지

    elif count == 3:
        st.balloons()
        st.success("🎉 완벽 실천! 오늘의 환경 히어로!")
        st.image("clap_big.png", width=250)  # 풍선+박수 이미지

    # --------------------
    # 오늘 실천한 항목 표시
    # --------------------
    st.write("### 🌱 오늘 실천한 내용")
    if check_cup:
        st.write("✔️ 양치할 때 컵 사용하기")
    if check_shower:
        st.write("✔️ 샤워 시간 줄이기")
    if check_tap:
        st.write("✔️ 수도 잠그기")
    
    # 날짜 다시 표시
    st.caption(f"📅 오늘 날짜: {today}")
